node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
backups/*.sql

# Local env files — secrets must never be committed
# Railway injects all secrets at runtime via environment variables
.env
.env.local
.env.*.local
.env.production.local
.env.development.local
.env.test.local
