name = "nexus-domain-proxy"
main = "nexus-domain-proxy.js"
compatibility_date = "2024-01-01"

[vars]
NEXUS_ORIGIN = "https://sospages.replit.app"

# After deploying, add a custom domain in the Cloudflare Workers dashboard
# so clients can CNAME their domain to e.g. proxy.spotonnexus.com
# Example: Workers & Pages -> nexus-domain-proxy -> Settings -> Custom Domains
